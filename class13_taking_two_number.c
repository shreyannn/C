#include <stdio.h>

int main(){
    int num1,num2;
    printf("enter two numbers:");
    scanf("%d%d",&num1,&num2);     // we can enter two numbers with space between them. or we can write 2nd number in another line too.
    printf("hello");
    return 0;
}